{
  "name": "cli",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "bin": {
    "cli": "./dist/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./dist",
    "build": "tsup",
    "old_build": "pnpm clean && tsc -b"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.6.3"
  }
}
